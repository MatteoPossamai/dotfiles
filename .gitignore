# never commit machine-local config or secrets
.zsh_secrets
.zsh_local

# editor / OS noise
*.bak
*.swp
.DS_Store

# antidote generated cache
plugins.zsh

# never track personal/employer git identity
.gitconfig
git/
